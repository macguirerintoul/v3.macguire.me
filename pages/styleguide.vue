<template>
  <section>
    <h1>Style Guide</h1>
    <p>
      This page specifies styling and semantic HTML rules to ensure consistent
      visual design throughout the site.
    </p>

    <h2>Type</h2>
    <p>
      For page headings (e.g. 'Style Guide'), use <code>h1</code>. According to
      the HTML standard, there should only be one <code>h1</code> per page.
    </p>

    <p>For sub-headings, use <code>h2</code> and <code>h3</code>.</p>
  </section>
</template>
